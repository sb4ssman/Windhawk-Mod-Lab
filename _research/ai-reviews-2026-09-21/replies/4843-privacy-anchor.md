<!-- Draft reply for PR #4843. Post ONLY after the user live-tests the build
     and the push lands. Post from PowerShell, never Git Bash. -->

Thanks for the clean review. The optional items were cheap, so they're all taken.

**The dead-code note.** You were right, and thanks for correcting the record: last round's reply overstated what was removed. The mod is assembled from shared components, and those helpers sat inside components the mod uses. The assembler now prunes a component's optional entry points by reference. I checked this push file: `ple::Lease::Abandon`/`Count`/`Empty`, `clr::ParseBrush`, the anchor overload of `lease_column::Acquire` and `Metrics::alongDip` are not in it. `<winrt/Windows.UI.Text.h>` is removed too.

**Optional items:**
- **The text callback vs. its comment:** the code now does what the comment says. The new type is written to the entry *before* the first `SetPrivacyActive`, no pointer into `g_privacyStates` is held across either call, and the entry is re-found afterwards.
- **Cross-thread read of `g_syntheticGrid`:** the phase-1 retry loop now reads `g_syntheticBarLive`, a `std::atomic<bool>` set where the bar is published and cleared where it is removed.

**Functionality note, taken: the stale restore snapshot.** The property lease gained `Refresh(object, property)`, which re-reads the local value into an existing snapshot. The visibility callback calls it for writes that are Windows', not the mod's. Every Visibility write the mod makes on a native icon is now marked (`g_ownVisibilityWrite`), and the callback ignores those. A Windows write to either value refreshes the snapshot before anything is re-collapsed. So unloading while an indicator is in use hands back what Windows last set, not what it had set when tracking began. The flag also replaces the old function-local re-entrancy guard, which covered only the restore path.

Generated by AI. Model: Claude Opus 5
