<!-- Draft reply for PR #4443. Post ONLY after the user live-tests the build
     and the push lands. Post from PowerShell, never Git Bash. -->

Thanks for the clean review. The optional items were small, so all of them are taken.

**Optional items:**
- **`Text` callback guard:** the property-changed callback now has the same `try`/`catch` as the two hooks, so nothing escapes into SystemTray's `Text` setter.
- **`g_settings` race:** `LoadSettings()` now runs inside the UI-thread dispatch in `Wh_ModSettingsChanged`, where every clock tick reads it. It loads directly on Windhawk's thread only when no taskbar thread is reachable, so no tick can be reading.
- **Uninit wait:** `Wh_ModUninit` returns at once when no tray module was hooked, so nothing can have been registered. It also stops the dispatch loop as soon as there is no taskbar window. It still retries briefly only when a window exists but the dispatch fails.
- **`@description`:** now "Companion for Taskbar Clock Customization: a %s% token for explicit, weightable elastic gaps between clock items (unlike its built-in Justified alignment, which stretches every space). Windows 11 only."

**Functionality notes, both taken:**
- **A line hidden in TCC:** when the source block is collapsed by someone other than this mod, `UpdateSpacerLine` removes any generated panel and leaves the line alone. `sourceCollapsed` marks the mod's own collapse. So hiding a line in TCC keeps it hidden even with `%s%` in its format. The README's Limitations section says so.
- **First-tick warning:** the "no spare width" line is logged only after the width has read zero for three consecutive evaluations, so the undefined order between the two mods' `OnApplyTemplate` hooks can't trigger it.

Generated by AI. Model: Claude Opus 5
