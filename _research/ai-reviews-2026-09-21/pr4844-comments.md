===== sb4ssman 2026-09-21T17:51:26Z
/ai-review

===== sb4ssman 2026-09-21T17:52:27Z
Thanks for the careful trace — both blocking items were real, and item 2 is why we removed the bridge rather than patching it.

**1 + 2. The 1.7 bridge is removed.** `HasLegacySettings`, `PreferCurrentOrLegacy*` and `Behavior.UseLegacySettings` are gone. Your item 2 is the root problem, and neither of the two fixes you offered removes it:
- Windhawk has no API to write a setting. `Wh_SetIntValue`/`Wh_SetStringValue` are mod storage and invisible to the settings page. So any bridge, including "until first save" or a storage snapshot, leaves a stretch where the settings page shows one value and the bar uses another.
- The "default means unset" rule makes a carried value impossible to set back to its default (your `beforeClock` → `afterClock` example).
- The mapping gaps in item 1 (`"dot"`, `gridVerticalOffset` → `PadY`, the Task View keys) showed that a partial bridge produces *wrong* results. A wrong migration is worse for a user than an obvious one-time reset.

The README's "Upgrading from 1.x" section, which you noted contradicted the bridge, is now accurate again. It says settings reset once on update, and now also tells users to copy their settings in Textual mode first. This also makes your "please test Save with undeclared keys" question moot.

**Optional items taken:**
- **Dead template code:** the mod is now assembled from eight small components, with its own focused tree walk (5,432 → 5,119 lines). Gone from the file: `property_lease`, `ContentAlong`/`AvailableRows`/`PixelsToDip`/`ResolveArrangement`/uncached `Measure`/`Arrange`, `vtw::*`, `sio::LoadString`/`sio::StringSetting` (now `WindhawkUtils::StringSetting`), `GetWindowsAccentBrush` and `VdPositionName`, along with the commentary about other mods and lab files.
- **Retry:** it now **uses** `RetryLoop`; the hand-rolled `CreateThread` worker is gone.
- **Deferred settings reapply freezing the bar:** fixed as you suggested. A reapply that could not run sets `g_reapplyPending`. While it is set, each retry attempt removes and rebuilds the old bar rather than treating "a grid exists" as done. The notification thread is restarted meanwhile, so the old bar keeps following desktop switches.
- **`RunFromWindowThread` running a callback once per concurrent hook:** fixed in the shared dispatch component with a `ran` flag in `Dispatch`, so every mod built from it gets the fix.
- **`!tornDown` branch:** the code now matches the rationale. Handler records hold strong XAML references, so they are retained with the other owners rather than cleared from Windhawk's thread. The preview window class is unregistered from the current thread when the popup's own thread can't be reached (it fails harmlessly if a window still exists).
- **`s_reapplied`:** replaced by a small struct passed through the `void*`. `InvalidateButtonGridSizeEstimate()` now runs on the UI thread, next to `RemoveButtonGrid()`.
- **Small stuff:** duplicate includes and `<thread>` removed; `<dwmapi.h>`/`<shobjidl.h>` moved to the top; the "NEW in v2.1" comment rewritten; the init log uses `WH_MOD_VERSION`. The embedded README's lab-relative link now points at the repository, and our README sync tooling does that for every lab link from now on.

**Functionality notes:**
- **Previews on secondary taskbars:** fixed. The hosting taskbar is now whichever of this process's taskbars contains the cursor (the hover that scheduled the preview is over the button), instead of a XamlRoot match that only works on the primary taskbar.
- **Untitled windows** are now drawn and counted, just without a caption. **Past the 48-window cap**, the extra windows are still counted in the footer. **`DWM_TNP_SOURCECLIENTAREAONLY`** is no longer set: `rcSource` is computed from the window rect, and the frame-included default is what we want.
- **Left as is:** the per-hover `CoCreateInstance` and registry reads. They run once per hover after the 400 ms delay, and caching a COM object in a namespace-scope holder would add a thread-affine release to unload. We also kept the 100 ms cursor poll while the popup is up. `PointerExited` covers the normal exit, but the poll is the backstop when the pointer leaves through the popup, which is `HTTRANSPARENT`, or when the button is rebuilt underneath it.

**Not changed, with reasons:**
- **`-loleaut32` is required.** `LogCurrentUiException` calls `winrt::hresult_error::message()`, which C++/WinRT stores in a `BSTR`. Removing the flag fails the link on `SysStringLen`/`SysFreeString`.

Generated by AI. Model: Claude Opus 5


===== windhawk-reviewer[bot] 2026-09-21T18:28:50Z
<!-- ai-review sha=b5a2440389721b18580a24c75efd7a8272600313 -->

### Submission review

_Note: This review was done by Claude. Due to the amount of submissions, doing a fully manual review for each pull request is no longer feasible. Thank you for understanding._

Remember: The AI reviewer can be wrong - it may misread code, flag correct code as broken, or suggest changes that make things worse. Treat its findings as suggestions to verify, not instructions to follow blindly. You're responsible for the code you submit, so if a finding doesn't hold up, say so instead of changing working code to satisfy it.

Please address the following issues. The items in the collapsed sections are optional, so it's your call whether to address them.

---

Both blocking items from the last round are resolved: the 1.7 bridge is gone along with `Behavior.UseLegacySettings`, and the README's "Upgrading from 1.x" now says what actually happens (one-time reset, copy your Textual-mode settings first). Removing the bridge rather than patching it is a sound call given that Windhawk has no API to write a setting. The optional items landed too — `RetryLoop` is now the retry worker, `g_reapplyPending` closes the frozen-bar case, the `ran` flag closes the double-dispatch, the `!tornDown` branch now matches its own comment, and the secondary-taskbar preview anchor uses the cursor. The unload path (switch-thread join → retry stop → notification stop → UI-thread teardown with per-step guards → optional resets on the UI thread) checks out. One thing is left, in the taskbar-rebuild path that was added in an earlier round:

**1. `OnTaskbarRebuilt` never lets go of the old grid, so if the immediate reapply fails, nothing comes back for it.**

`TrayUI::StartTaskbar` fires when Explorer reconstructs the taskbar in place; the old XAML tree is gone. `OnTaskbarRebuilt` (line 4865) clears `g_taskbarWnd` and calls `ApplyAllSettingsOnWindowThread()` once, synchronously, from inside the `StartTaskbar` hook — but leaves `g_buttonGrid`, `g_injectionParent`, the Start-overlay globals and the old `ButtonEventState` records pointing at the dead tree. Two consequences:

* **The fallbacks are blocked.** At that point in `StartTaskbar` the tray XAML is typically not populated yet (this is the same reason initial injection waits for `IconView` → `Loaded`), so `ApplyAllSettings` is likely to hit "GetTaskbarXamlRoot failed" / "SystemTrayFrameGrid not found" and return. After that, `IconView_IconView_Hook` early-returns on `if (g_buttonGrid && !g_settings.allTaskbars)` (line 4803) because the *stale* `g_buttonGrid` is non-null, and the retry loop is not re-armed (`ReapplyOnWindowThread` at line 4944 also only applies when `!g_buttonGrid`). The bar stays missing until the next desktop switch/create/destroy notification, whose `RebuildButtonGrid` is the only path that detects "genuinely gone" and reinjects.
* **When the immediate apply does succeed**, `InjectButtonGrid` overwrites `g_buttonGrid` (line 4349) without clearing the old grid's records, so every rebuild leaks one grid's worth of `ButtonEventState` entries (strong `Grid`/`ButtonBase` refs with delegates still attached) into `g_buttonEventStates` until unload.

The pieces to fix this already exist: the `g_reapplyPending` path does exactly the right thing here (`RemoveButtonGrid()` finds no bar in the live tree, clears the old records, nulls the globals — revoking the old `LayoutUpdated` token in Start mode too — then `ApplyAllSettings()` + `RefreshSecondaryBars()`), and `RetryLoop::Start` is designed to be callable from the UI thread. So:

```cpp
static void OnTaskbarRebuilt() {
    if (g_unloading) return;
    g_taskbarWnd = nullptr;
    // The old tree is gone with the bar in it. Treat this like a deferred
    // reapply: the retry removes whatever is left of the old bar and rebuilds
    // once the new tray XAML is reachable.
    g_reapplyPending = true;
    StartRetryThread();
}
```

(The first attempt runs immediately, so the separate `ApplyAllSettingsOnWindowThread()` call becomes redundant.) [taskbar-multi-tray.wh.cpp#L4915-L4926](https://github.com/ramensoftware/windhawk-mods/blob/main/mods/taskbar-multi-tray.wh.cpp#L4915-L4926) does the same thing for this hook — an immediate apply plus a deferred, retried one — with a UI-thread `SetTimer`, which is an equally valid shape if you'd rather not touch the worker from the taskbar thread.

While there: the `RetryLoop` comment at lines 1845–1846 says "both of this mod's callers run on the taskbar's UI thread" — today both callers (`Wh_ModAfterInit`, `Wh_ModSettingsChanged`) run on Windhawk's thread, and with the change above one moves to the UI thread. The exchange-and-stop logic in `Start()` already handles concurrent starts, so only the comment needs fixing.

<details><summary>Optional improvements</summary>
<p>

Minor polish — none of this affects users, so it's your call.

* **Dead code — the reply says these were removed, but they're still in the file.** `sio::LoadString` (line 654), `ngl::ContentAlong` (line 809), the uncached `ngl::Measure` (line 1132) and `ngl::Arrange` (line 1214), and `ngl::ResolveArrangement` with its `Arrangement` struct (lines 1434–1459) are defined and never called. `<climits>` (line 580) is also unused. The rest of the trim did land (`property_lease`, `vtw`, `PixelsToDip`, `sio::StringSetting`, `GetWindowsAccentBrush`, `VdPositionName` are gone).

* **`[[clang::no_destroy]]` on `g_retry` (line 2220) is unnecessary.** `RetryLoop`'s implicit destructor is `~std::mutex` (trivial on Windows) plus a `shared_ptr` release whose `~Run` only calls `CloseHandle` — nothing blocks, terminates, or needs a specific thread, so it is safe at process shutdown and the attribute is noise (the comment even says so). Per https://github.com/ramensoftware/windhawk/wiki/Global-objects-and-process-shutdown, suppress only where the destructor is dangerous; the other attributes in the file (XAML refs, and the three `std::optional` containers of XAML refs) are all justified and correctly released on the UI thread in `Wh_ModUninit`.

* **`LoadSettings()` still races UI-thread readers on a settings change.** The comment at line 5068 stops the retry and notification threads before rewriting the `std::wstring` members, but the UI thread has readers of its own that aren't stopped: an `IconView` `Loaded` handler (line 4828/4834) can call `ApplyAllSettingsOnWindowThread()` or `RegisterSecondaryTrayFromElement()` → `BuildButtonGrid()`, which reads every string in `g_settings`, while `LoadSettings()` is mid-assignment on Windhawk's thread. Rare (a tray icon has to load during a save), so it's optional — but since every reader is UI-thread-only, the simplest fix is to run `LoadSettings()` inside the UI-thread dispatch in `Wh_ModSettingsChanged` (and in `ReapplyOnWindowThread` when the dispatch was deferred), rather than before it.

* **Two integer settings reach XAML unclamped.** `Surface.FontSize` (line 2159 → `FontSize()` at line 3680) and `Surface.CornerRadius` (line 2166 → `CornerRadius{}` at line 3689) throw for 0 / negative values; the exception is caught, but the bar silently disappears with only the generic "Exception during injection (XAML not ready)" log line. A `std::max(1, …)` / `std::max(0, …)` like the neighbouring `Size.*` reads avoids the confusing failure mode.

* **Small stuff**: `ModSettings::position` defaults to `BeforeOmni` (line 2013) while the declared default and `LoadChoice` fallback are `afterClock` — harmless since `LoadSettings` always runs first, but worth aligning.

</p>
</details>

<details><summary>Functionality notes</summary>
<p>

Non-critical observations and ideas about the feature behavior itself.

* **The preview popup's text doesn't scale with DPI.** `Show()`/`WM_PAINT` scale `inset`, `heading`, and the thumbnail box by `RasterizationScale`, but the captions and footer are drawn with `DEFAULT_GUI_FONT` (line 2939), which is a fixed ~8pt-at-96dpi stock font. On a 150–200 % monitor the heading box grows while the glyphs stay tiny. `SystemParametersInfoForDpi(SPI_GETNONCLIENTMETRICS, …, GetDpiForWindow(popup))` → `CreateFontIndirectW(&ncm.lfMessageFont)` (or `lfCaptionFont`) gives a correctly scaled shell font; select it per paint and delete it afterwards, like `ScopedBrush` does for brushes.

* **`GetWindowTextW` in `Collect` (line 2839) can stall the taskbar on a hung same-process window.** For windows owned by *another* process it reads the cached caption and never blocks, but every Explorer file window (`CabinetWClass`) lives in this process on its own thread, and for those it sends `WM_GETTEXT` synchronously — so a File Explorer window stuck on a slow network share would freeze the taskbar for the duration of the hover. `SendMessageTimeoutW(window, WM_GETTEXT, …, SMTO_ABORTIFHUNG | SMTO_BLOCK, 100, …)` for same-PID windows keeps the preview responsive (an aborted read just yields an untitled thumbnail, which the popup already handles).

* **Previous-round items you chose to leave as they are** (per-hover `CoCreateInstance` + registry reads, the 100 ms cursor poll while the popup is up) are fine as decided; not repeating them.

</p>
</details>

<details><summary>Code overview</summary>
<p>

For the maintainer: a map of the mod and what it touches outside its own process. Descriptive only — anything that needs a change is listed above. Line numbers refer to the mod source at commit `b5a2440`.

**Code regions**

* Line 1–11: metadata — `@include explorer.exe`, `x86-64`, links ole32/oleaut32/runtimeobject/version/uuid/dwmapi/gdi32.
* Line 13–319: README — screenshots on `raw.githubusercontent.com`, the Arrangement mini-language, hover previews, Task View button, settings tables, Taskbar Styler selector, "Upgrading from 1.x" (one-time settings reset).
* Line 321–553: settings block — eight nested groups (Placement, Content, Layout, Size, Adjust, Surface, State, Behavior); all keys are read in `LoadSettings` with matching types.
* Line 555–597: includes and `using` directives (C++/WinRT XAML, dwmapi, shobjidl, winver).
* Line 599–662: `vd_switcher_settings` — clamped int/bool reads, `LoadChoice` table for `$options` values, an unused `LoadString`.
* Line 664–744: `vd_switcher_color_tokens` — parses `#RRGGBB`/`#AARRGGBB`, `accent*` tokens (via `UISettings`), and `transparent` into a `Color`/`SolidColorBrush`; empty/invalid → "leave native".
* Line 746–1461: `vd_switcher_layout` — the Arrangement expression parser (`|`, `,`, parentheses, `[dx,dy]` offsets, depth cap 24), memoized measure/arrange with axis-relative sizing for the Task View button, auto-shape selection, and append-missing-tokens policy.
* Line 1463–1498: `vd_switcher_taskbar_window` — PID-filtered `EnumWindows` for this process's `Shell_TrayWnd`, cached-handle validation.
* Line 1500–1605: `vd_switcher_dispatch` — `RunFromWindowThread`: per-call `WH_CALLWNDPROC` thread hook + `RegisterWindowMessage` + `SendMessage`, reports whether the callback ran; `ran` flag guards concurrent dispatches.
* Line 1607–1727: `vd_switcher_taskbar_xaml` — `taskbarDllHooks` (taskbar.dll: `CTaskBand` vftable, `GetTaskbarHost`, `TaskbarHost::FrameHeight`, `_Ref_count_base::_Decref`, and a `TrayUI::StartTaskbar` hook that calls the mod's rebuild callback), `FrameworkElementOffset` read from the `FrameHeight` prologue (x64/ARM64), `GetTaskbarXamlRoot`.
* Line 1729–1819: `vd_switcher_taskbar_metrics` — taskbar rect → DIPs, orientation detection; vertical taskbars make the mod stand down.
* Line 1821–1980: `vd_switcher_retry` — `RetryLoop`: `CreateThread` worker with a shared `Run` (thread + stop event), bounded attempts, pumped `MsgWaitForMultipleObjects` stop, exchange-and-stop publication.
* Line 1984–2192: namespace aliases, `ModSettings` (heap-only, no `no_destroy`), `LoadSettings` — every setting parsed once into enums/ints/strings.
* Line 2193–2258: globals — XAML refs under `[[clang::no_destroy]]`, three `no_destroy std::optional` containers (Loaded revokers, button handler records, secondary bars), thread handles, `g_retry`, `g_reapplyPending`/`g_applySettled`, forward declarations.
* Line 2260–2334: explorer.exe / twinui.pcshell.dll build detection from version resources; resolves the tray module (SystemTray.dll → Taskbar.View.dll < 2604 → ExplorerExtensions.dll).
* Line 2336–2393: mod-side wrappers — `LogCurrentUiException` (rethrow-and-catch logger), `RunFromWindowThread`/`GetTaskbarXamlRoot` shims, `FindChildRecursive`.
* Line 2395–2592: virtual-desktop notification sink — hand-built COM vtable sized per build (`GetNotifConfig`), STA notification thread registering with `IVirtualDesktopNotificationService` via `CLSID_ImmersiveShell`; desktop events dispatch `RebuildButtonGrid` to the UI thread; `StopNotificationThread`, `StopRetryThread`, `WaitForSwitchThreads` (pumped joins).
* Line 2594–2684: registry readers for desktop count, current desktop GUID, and per-desktop display names.
* Line 2686–3083: `desktop_preview` — hover overview popup: theme palette from `Themes\Personalize`, own window class (`WindhawkDesktopPreview_<modid>`, registered once and tracked separately from the popup), `EnumWindows` + `IVirtualDesktopManager::GetWindowDesktopId` to collect the desktop's windows, DWM thumbnails composited to scale, timers for delay and cursor polling, `Destroy`/`UnregisterClassIfRegistered` for teardown.
* Line 3085–3164: `SwitchToDesktop` — `IVirtualDesktopManagerInternal` via `CLSID_ImmersiveShell` with build-specific IIDs and vtable slots 7/9 (`GetDesktops`/`SwitchDesktop`).
* Line 3166–3529: labels (`ToRoman`, custom labels, symbols), shine gradient, layout glue (`AvailableRows` in DIPs, token vocabulary, Task View placement wrapping, `ComputeButtonPlacements` with auto-fallback and append-missing, cached size estimate for Start mode).
* Line 3531–3715: button styling — lightweight `ToggleButton*`/`Button*` resource keys for normal/hover/pressed/checked states, geometry, fonts, border, corner radius.
* Line 3717–3883: `BuildButtonGrid` — absolute-positioned `Grid` named `VdSwitcherBar`; per desktop a `ToggleButton` (`VdBtn_N`) with `Click` → desktop-switch worker thread (reaped per click, joined on unload) and hover-preview `Pointer*` handlers; optional Task View `Button` whose `Click` sends Win+Tab; all handler tokens recorded in `g_buttonEventStates`.
* Line 3885–4142: Start-placement injection — overlay into the taskbar `RootGrid`, pushes `TaskbarFrameRepeater.Margin.Left` and counter-translates the Start button (restored on removal), `LayoutUpdated` repositioning.
* Line 4144–4357: tray injection — classifies `SystemTrayFrameGrid` as `Grid` (inserts an Auto column, shifts siblings' `Grid.Column`/`ColumnSpan`) or `StackPanel` (child index, 26200.9457+); `InjectButtonGrid` with re-acquire of an existing bar.
* Line 4359–4510: `FindLiveSystemTrayFrameGrid`, `ClearButtonEventState` (revokes delegates, clears tooltip/content), `RemoveButtonGridFrom` (undoes the column insert), `RemoveButtonGrid` (both modes).
* Line 4512–4633: secondary taskbars — discovered from `IconView` `Loaded` XamlRoots, refreshed/rebuilt on each desktop event, injected only when `Placement.AllTaskbars` is on.
* Line 4635–4732: `RebuildButtonGrid` — full rebuild on each desktop event, with "deferring" and "genuinely gone → reinject" branches.
* Line 4734–4788: `ApplyAllSettings` — vertical-taskbar stand-down, XamlRoot lookup, inject, start the notification thread; `ApplyAllSettingsOnWindowThread`.
* Line 4790–4855: hooks — `IconView::IconView` (defers to `Loaded` via `g_autoRevokerList`; first-injection trigger and secondary-taskbar discovery) and `LoadLibraryExW` resolved from kernelbase.dll for a late-loaded tray module.
* Line 4857–4895: `OnTaskbarRebuilt` (rebuild callback, see item 1), `systemTrayHooks` (`// SystemTray.dll, Taskbar.View.dll, ExplorerExtensions.dll` — one `HookSymbols` call per module, guarded by `g_systemTrayModuleHooked`), `HandleLoadedModuleIfSystemTray` → `Wh_ApplyHookOperations`.
* Line 4897–5119: Windhawk entry points — `Wh_ModInit` (settings, build detection, taskbar.dll hooks, tray-module hooks or kernelbase `LoadLibraryExW` hook), retry attempt glue (`ReapplyOnWindowThread`, `StartRetryThread`), `Wh_ModAfterInit` (apply + retry), `Wh_ModUninit` (join switch threads → stop retry → stop notifications → UI-thread teardown with per-step guards → `optional::reset()` on the UI thread; damage-limitation fallback if the UI thread is unreachable), `Wh_ModSettingsChanged` (`void` form; stop workers → reload → UI-thread reapply, or mark pending + restart workers).

**Side effects of interest**

* Line 2598–2684, 2977–2981: registry read: `HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\SessionInfo\<session>\VirtualDesktops` (fallback `…\Explorer\VirtualDesktops`) — `VirtualDesktopIDs`, `CurrentVirtualDesktop`; and `…\VirtualDesktops\Desktops\{GUID}\Name`. Read only; on every inject/rebuild and per hover.
* Line 2712–2722: registry read: `HKCU\…\Themes\Personalize` — `SystemUsesLightTheme`, `AppsUseLightTheme`. Read only; per popup paint/hover.
* Line 2503–2533: IPC: `CoCreateInstance(CLSID_ImmersiveShell, CLSCTX_LOCAL_SERVER)` on the notification thread → `IVirtualDesktopNotificationService::Register`/`Unregister` (cookie released on unload).
* Line 3126–3162: IPC: `CoCreateInstance(CLSID_ImmersiveShell, CLSCTX_LOCAL_SERVER)` on a per-click worker thread → `IVirtualDesktopManagerInternal` `GetDesktops`/`SwitchDesktop` by vtable slot (switches the active virtual desktop — the mod's purpose).
* Line 2850–2856: IPC: `CoCreateInstance(CLSID_VirtualDesktopManager, INPROC)` per hover → `GetWindowDesktopId` for every top-level window.
* Line 3781–3789: other: `SendInput` of Win+Tab on Task View button click (opens Task View).
* Line 2811–2857: other: `EnumWindows` **without** a PID filter, read-only (`GetWindowRect`, `GetWindowTextW`, `GetClassName`, `DwmGetWindowAttribute`), to collect the hovered desktop's windows.
* Line 2874–2895, 2800–2803: other: `DwmRegisterThumbnail` of foreign windows onto the mod's own popup; unregistered in `Hide()`.
* Line 1574–1601: other: `SetWindowsHookExW(WH_CALLWNDPROC)` scoped to the taskbar thread of this process, installed per dispatch and removed right after `SendMessage` returns.
* Line 3027–3057, 3065–3082: other: `RegisterClassW`/`CreateWindowExW` for the preview popup (`WS_EX_TOPMOST | WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW`, owned by the taskbar), `DwmSetWindowAttribute` on that popup only; `UnregisterClassW` on unload. Module handle obtained with `GET_MODULE_HANDLE_EX_FLAG_UNCHANGED_REFCOUNT` (no extra reference on the mod).
* Line 1644: other: `LoadLibraryExW(L"taskbar.dll", nullptr, LOAD_LIBRARY_SEARCH_SYSTEM32)` (already loaded in explorer.exe; search restricted to System32).
* No file writes, no registry writes, no network access, no system-settings changes; injection is limited to `explorer.exe`.

</p>
</details>

---

**Next steps:**

* `/ai-review` - after pushing fixes, to get a review of the updated code. You can repeat this as many times as you need, but each review is thorough and usually there's no need for more than 2-3 iterations.
* `/ready-for-reviewer` - once you're satisfied with the state of the pull request, to hand it over to a human reviewer. If some findings above are left unaddressed, add a short note explaining why.

See the [review process](https://github.com/ramensoftware/windhawk/wiki/Mod-submission-review-process) for details.

Important: Each reply that was generated by AI must be disclosed by including the following text in the reply: "Generated by AI. Model: (ai-model-name)".

