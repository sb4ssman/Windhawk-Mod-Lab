===== sb4ssman 2026-09-21T17:51:21Z
/ai-review

===== sb4ssman 2026-09-21T17:52:23Z
Thanks — and thanks for confirming there's no correctness or stability defect left.

**1. README.** Done, and it was worth doing. There is a new "Try the built-in option first" section right after the requirements, and `@description` now points at the difference too. It names Taskbar Clock Customization 1.8's **Text alignment → Justified** as the zero-install option: ordinary spaces for gaps, a non-breaking space inside items. It then lists what `%s%` adds, kept to the differences that hold up:
- gaps only where you put them. Justified stretches every space and can widen inter-character spacing, which the maintainer himself observed inside `MB/s`;
- weighted gaps (`%s%%s%`);
- a per-gap minimum width;
- `{spacer}` inside the Weather format.

The "Relationship" section now describes the upstream outcome accurately.

One factual correction on the framing, for the record. The review says the maintainer "has said twice … that they'd rather extend TCC than merge a companion". The upstream thread ends the other way. On 2026-06-29, in [m417z/my-windhawk-mods#68](https://github.com/m417z/my-windhawk-mods/pull/68), after testing the `Justify` approach, m417z wrote:

> "The most visible downside that I didn't notice before is that Windows adds spaces between characters, it's visible in "MB/s" for example - it has large gaps between characters. It works better with more than one space, but then it becomes a guessing game, so not ideal. […] So it doesn't work as well as I hoped. I also gave it some thought, and for now I think that creating additional TextBlock controls is out of scope for this mod. **We can merge https://github.com/ramensoftware/windhawk-mods/pull/4443 if you prefer, let me know.**"

So the maintainer offered to merge this PR after weighing the built-in alternative. The README now gives users the facts to choose between the two.

**Optional items, all taken:**
- The stale comments are fixed: the `StartTaskbar` "preferred path" paragraph now describes the `LoadLibraryExW` late-load path as what it is, and the template references and the validator note are gone.
- The scaffolding is flattened. The three single-use namespaces and their aliases are gone, and so are the forwarding wrappers and the `SetExceptionLogger` indirection (the dispatch now calls `Wh_Log` directly). `LoadInt` became `std::clamp` at the two call sites. The dispatch also gained a `ran` flag, so two concurrent dispatches cannot each run a callback twice.
- `ClearSpacerStates` guards each entry on its own and zeroes `textToken` right after unregistering, so the uninit retry is idempotent.
- The init log uses `WH_MOD_VERSION`.

**Functionality notes:**
- **Line height:** taken. `CopyTextStyle` now carries `LineHeight` and `LineStackingStrategy`.
- **`Spacing`:** deliberately *not* applied to the generated panel. An unspaced multi-line block is one TextBlock whose lines are separated only by line height. TCC's *Text spacing* is the shared panel's gap *between* the time and date blocks, not between lines, so copying it between our rows would add a gap the unspaced block doesn't have.
- **Build before record:** fixed. The state is pushed into `g_states` first and built in place, so a throw after the insert still leaves an entry that unload can remove.

Generated by AI. Model: Claude Opus 5


===== windhawk-reviewer[bot] 2026-09-21T18:51:52Z
<!-- ai-review sha=a78f6636134d6af68337b173c1fd1b575d72ea9f -->

### Submission review

_Note: This review was done by Claude. Due to the amount of submissions, doing a fully manual review for each pull request is no longer feasible. Thank you for understanding._

Remember: The AI reviewer can be wrong - it may misread code, flag correct code as broken, or suggest changes that make things worse. Treat its findings as suggestions to verify, not instructions to follow blindly. You're responsible for the code you submit, so if a finding doesn't hold up, say so instead of changing working code to satisfy it.

Please address the following issues. The items in the collapsed sections are optional, so it's your call whether to address them.

---

No blocking issues — looks good to merge.

This round closes out the previous review. The README now leads with Taskbar Clock Customization's own *Justified* alignment and lists the concrete residual (`%s%` gaps only where placed, weighted `%s%%s%`, a per-gap minimum, `{spacer}` in the weather format), and `@description` says the same. On the framing correction: verified against the upstream thread — on 2026-06-29 the maintainer, after testing the `Justify` approach, wrote that extra TextBlock controls are out of scope for that mod and "We can merge ramensoftware/windhawk-mods#4443 if you prefer", so the earlier characterization of the maintainer's position was wrong and the overlap question is settled by the maintainer's own words. The optional items from last round all landed as described: the scaffolding is flat, the stale comments are gone, `ClearSpacerStates` guards each entry and zeroes `textToken` so the uninit retry is idempotent, `SetupSpacerForTextBlock` records before building, `CopyTextStyle` carries `LineHeight`/`LineStackingStrategy`, and the init log uses `WH_MOD_VERSION`. I re-walked the whole file at `a78f663` against [taskbar-clock-to-left](https://github.com/ramensoftware/windhawk-mods/blob/main/mods/taskbar-clock-to-left.wh.cpp#L1569-L1634) (same module selection, same two symbols with the same mandatory/optional split, same `pThis` layout) and against TCC's [`ApplyStackPanelStyles`/`ApplyTextBlockStyles`](https://github.com/ramensoftware/windhawk-mods/blob/main/mods/taskbar-clock-customization.wh.cpp#L4220-L4373) (it does put *Max width* on the shared `StackPanel.MaxWidth`, and only re-sets `Visibility` on a style change, not per tick), and found nothing that affects users. Every `g_states` access is on the taskbar UI thread, the globals are all shutdown-safe (weak refs, ints, atomics — no `no_destroy` needed, and none is used), and the unload path unregisters the XAML callbacks on the owning thread before the image goes away.

<details><summary>Optional improvements</summary>
<p>

Minor polish — none of this affects users, so it's your call.

- **Wrap the `Text` property-changed callback body in `try`/`catch` like the two hooks (lines 819-829).** The callback is a C++/WinRT delegate, so an `hresult_error` escaping `UpdateSpacerLine` isn't lost — it's converted to a failed HRESULT and handed back to XAML, which surfaces it inside SystemTray's own `Text` setter, i.e. in Explorer's code rather than yours. The hooks already guard against exactly this (lines 871-875, 894-912); the callback is the one entry point that doesn't. `UpdateSpacerLine` only touches live objects so it's very unlikely to throw, but the guard is three lines and makes all three entry points consistent.

- **`g_settings` is written on the Windhawk thread and read on the UI thread every tick (lines 345-350 vs. 489, 557, 749).** `Wh_ModSettingsChanged` runs `LoadSettings()` on the settings thread and only *then* dispatches the refresh; a clock tick in between reads `maxWidth`/`minSpacerWidth` while they're being written. Aligned 32-bit stores don't tear on x64 so this can't misbehave in practice, but formally it's a race that exists only on a settings change. Either make the two members `std::atomic<int>`, or move `LoadSettings()` into the dispatched lambda so everything that reads `g_settings` runs on the UI thread (keeping the direct call as a fallback when there's no taskbar window).

- **`Wh_ModUninit` sleeps 500 ms when there is nothing to clean up (lines 1090-1095).** The retry loop `Sleep(100)`s five times even when `FindCurrentProcessTaskbarWnd()` returns null on the first pass — a taskbar window that isn't there won't appear during unload. In a non-shell `explorer.exe` (file windows in a separate process), SystemTray.dll never loads, there's no `Shell_TrayWnd`, and every disable/update of the mod pays the full half second for nothing. Break out when there's no window, or skip the loop entirely when `!g_systemTrayModuleHooked` — with no hooks there can be no states.

- **`@description` reads awkwardly as a one-liner (line 4).** "…a %s% token for explicit, weightable gaps between clock items, where its built-in Justified alignment stretches every space" — the "its" is TCC and the clause is a comparison, which is hard to parse in the catalog list without the README. Something like "Companion for Taskbar Clock Customization: a %s% token for explicit, weightable elastic gaps between clock items (unlike its built-in Justified alignment, which stretches every space). Windows 11 only." keeps the contrast and reads on its own.

</p>
</details>

<details><summary>Functionality notes</summary>
<p>

Non-critical observations and ideas about the feature behavior itself.

- **A line hidden in TCC still shows its generated rows.** TCC's *Hidden* option collapses the block and force-collapses it again on any `Visibility` change ([TCC#L4249-L4262](https://github.com/ramensoftware/windhawk-mods/blob/main/mods/taskbar-clock-customization.wh.cpp#L4249-L4262)). This mod keys only on the text: if that hidden line's format contains `%s%`, `UpdateSpacerLine` still builds and shows the generated panel, so the user hides the time line in TCC and it comes back — as spaced rows. A cheap check is to skip (and remove any existing panel) when `original.Visibility()` is already `Collapsed` at the moment the mod first sees the block (`!state.sourceCollapsed`); after that, TCC's own callback keeps re-collapsing while the mod's collapse is a no-op. Unlikely combination, but it's a visible surprise if hit.

- **First-tick width warning may be spurious.** Both TCC and this mod act from the same `OnApplyTemplate` hook and the order between two mods' hooks on one function isn't defined, so on the first `Text` change TCC may not have set `MaxWidth` on the panel yet; `WarnIfNoElasticRoom` then logs its one-shot "no spare width" line even though the next tick reads the width fine. Cosmetic (the log is off by default), but if you want the warning to stay meaningful, arm it only after the width has been seen non-zero once, or after a couple of ticks.

</p>
</details>

<details><summary>Code overview</summary>
<p>

For the maintainer: a map of the mod and what it touches outside its own process. Descriptive only — anything that needs a change is listed above. Line numbers refer to the mod source at commit `a78f663`.

**Code regions**

- Line 1–11: metadata — `@include explorer.exe`, `@architecture x86-64`, links `ole32`/`oleaut32`/`runtimeobject`/`version`.
- Line 13–162: README — the two requirements (TCC installed; a fixed clock width), "Try the built-in option first" with the `%s%`-vs-Justified residual (43–60), token table, `{spacer}` for the weather format, setup, troubleshooting, how it works, relationship to TCC. Image on `raw.githubusercontent.com`.
- Line 164–179: settings block — `maxWidth`, `minSpacerWidth` (both numbers; both read in `LoadSettings`).
- Line 181–203: includes and `using namespace` for XAML/Controls/Media.
- Line 209–225: `FindChildRecursive` — depth-bounded DFS by predicate over the clock's own subtree.
- Line 237–254: `FindCurrentProcessTaskbarWnd` — `EnumWindows` filtered to the current PID, returns `Shell_TrayWnd`.
- Line 256–328: UI-thread dispatch — `InvokeUiCallback` (try/catch + log), `Dispatch` with a `ran` flag against double-execution, `RunFromWindowThread` (per-thread `WH_CALLWNDPROC` hook + `RegisterWindowMessageW("Windhawk_RunFromWindowThread_" WH_MOD_ID)` + synchronous `SendMessageW`, hook removed right after).
- Line 334–358: `ModSettings`, `LoadSettings` (clamped 0–4000), `CurrentLayoutKey` folding both settings into a rebuild key.
- Line 364–396: globals — `g_unloading`/`g_systemTrayModuleHooked`/`g_warnedNoElasticRoom` atomics, token constants (`%s%`, `{spacer}`), `SpacerState` (weak refs to original block, parent panel, generated panel + token/flags), `g_states` vector.
- Line 402–450: token search and splitting — `FindNextSpacer`, `HasSpacerToken`, `SplitOnSpacer`, `SplitLines` (CRLF-aware).
- Line 452–540: `CopyTextStyle` (font/colour/alignment/line-height), `ApplySegmentAlignment` (first left, last right, middle centre), `EffectiveLineWidth` (mod's `maxWidth`, else TCC's `StackPanel.MaxWidth`, else 0), panel/row width constraint helpers, one-shot "no elastic room" log.
- Line 547–609: `BuildSpacerGrid` (Auto/Star/Auto… columns, `MinWidth` on star columns from `minSpacerWidth`) and `BuildLineElement` (grid when the line split, plain `TextBlock` otherwise).
- Line 626–678: fast path — `UpdateLineElementText`/`UpdateGeneratedPanelText` rewrite text and re-copy style/width in place when the shape is unchanged.
- Line 687–721: `CollapseSourceTextBlock` (zero size + Collapsed), `RestoreSourceTextBlock` (`ClearValue` on the same six properties, only if the mod collapsed it), `RemoveGeneratedPanel`.
- Line 727–788: `UpdateSpacerLine` — per-line driver: no token → remove/restore; else fast path, or rebuild the vertical `StackPanel` and insert it at the original's index in TCC's shared panel.
- Line 794–852: registration — `SetupSpacerForTextBlock` (dedupe, prune expired entries, push state, build, register `TextProperty` changed callback), `ApplySpacerToDateTimeContent` (finds `TimeInnerTextBlock`/`DateInnerTextBlock`, requires a `StackPanel` parent).
- Line 858–915: symbol hooks — `DateTimeIconContent::OnApplyTemplate` (calls original, QIs `pThis[1]` to `FrameworkElement`, applies) and `BadgeIconContent::get_ViewModel` (optional; gated on runtime class `SystemTray.DateTimeIconContent` and `IsLoaded()`).
- Line 917–949: `GetModuleVersionInfo` and `GetSystemTrayModuleHandle` — `SystemTray.dll`, else `Taskbar.View.dll` only if file version < 2604, else `ExplorerExtensions.dll`.
- Line 956–985: `HookSystemTraySymbols` — one `HookSymbols` call, array declared `// SystemTray.dll, Taskbar.View.dll, ExplorerExtensions.dll` (matches the three candidates above); `TryHookSystemTrayModule` with an `exchange` guard then `Wh_ApplyHookOperations`.
- Line 990–1005: `LoadLibraryExW` hook (late-load path) — compares the loaded module to `GetSystemTrayModuleHandle()` and hooks once it matches.
- Line 1017–1037: `ClearSpacerStates` — per entry, guarded: unregister callback (token zeroed), restore source block, remove generated panel; then clears the vector.
- Line 1043–1116: lifecycle — `Wh_ModInit` (hook the tray module if loaded, else hook kernelbase `LoadLibraryExW` via `GetProcAddress`), `Wh_ModAfterInit` (retry hooking), `Wh_ModUninit` (set `g_unloading`, dispatch `ClearSpacerStates` to the taskbar thread with up to 5×100 ms retries), `Wh_ModSettingsChanged` (reload, dispatch a refresh of all lines).

**Side effects of interest**

- Line 237–254: other reach outside the process: `EnumWindows` — filtered to `GetCurrentProcessId()`, only ever returns the process's own `Shell_TrayWnd`; runs on settings change and on uninit.
- Line 302–326: other reach outside the process: `SetWindowsHookExW(WH_CALLWNDPROC)` scoped to the taskbar thread of the current process (thread id from `GetWindowThreadProcessId`), plus a `RegisterWindowMessageW` message `SendMessage`d to the process's own `Shell_TrayWnd`; hook is removed synchronously after the send. Runs on settings change and on uninit.
- Line 920–926: in-process read of `Taskbar.View.dll`'s version resource (`FindResource`/`VerQueryValueW`) to decide which module hosts the SystemTray types.
- Line 1059–1066: function hook on `kernelbase.dll!LoadLibraryExW` in the host process, installed only when the tray module isn't loaded at init.
- No external file access, no registry access, no network access, no IPC, no system-settings changes — everything else is XAML manipulation inside the process's own taskbar tree.

</p>
</details>

---

**Next steps:**

* `/ai-review` - after pushing fixes, to get a review of the updated code. You can repeat this as many times as you need, but each review is thorough and usually there's no need for more than 2-3 iterations.
* `/ready-for-reviewer` - once you're satisfied with the state of the pull request, to hand it over to a human reviewer. If some findings above are left unaddressed, add a short note explaining why.

See the [review process](https://github.com/ramensoftware/windhawk/wiki/Mod-submission-review-process) for details.

Important: Each reply that was generated by AI must be disclosed by including the following text in the reply: "Generated by AI. Model: (ai-model-name)".

