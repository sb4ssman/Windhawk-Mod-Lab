===== sb4ssman 2026-09-21T17:51:31Z
/ai-review

===== sb4ssman 2026-09-21T17:52:31Z
Thanks — the `g_stoodDown` regression was real. Addressed in this push:

**1. Transient states no longer retire the retry.** `g_stoodDown` is now set only for decisions another attempt cannot change: a vertical taskbar, every utility switched off, or a *measured* tray below `MinimumTrayHeight`. The three transient exits return `false` and keep the bounded retry alive:
- a tray with `ActualHeight() <= 0` (not arranged yet);
- no layout items yet;
- no placements yet.

The budget is now 20 attempts × 1.5 s, because "not populated yet" is now what the loop is waiting out.

Beyond the retry budget, as you suggested, each apply now records the visible-icon count of every *unmanaged* candidate host as a weak reference. The `LayoutUpdated` check compares those as well as the managed hosts, so a `MainStack` that fills in after a chevron-only apply re-runs the layout. To keep the added walk off the hot path, the check is throttled to 500 ms instead of 250 ms.

**Optional items:**
- **`RetryLoop::Start` re-entry:** already fixed. `run_` is published by `std::exchange` under the mutex, and whatever it displaces is stopped. This is the shape you proposed.
- **`no_destroy` on `g_retry`:** you're right that it guards no wait, and the comment now says so. The attribute stays because our exit-time-destructor audit requires every namespace-scope object with a non-trivial destructor to be either marked or proven heap-only. Removing it fails that check.
- **Dead code:** `FindDirectTrayHost` and the `FindCurrentProcessTaskbarWnd` wrapper are removed. `RestoreObject` and the two `Metrics` fields belong to components shared with sibling mods, where they are used; splitting the components per function would cost more than those few lines.
- **`Wh_ModSettingsChanged` without a taskbar** now loads the settings, so the next `StartTaskbar` rebuild uses them.
- **`Wh_ModAfterInit`** starts the same bounded retry instead of making a single attempt.
- **`AcquireAt` with a stale marker:** fixed in the shared lease component. A same-named marker that is *not* the caller's live lease is released and the slot is taken fresh, with the requested slot corrected if the released marker sat before it. The caller's own live lease is still refused, because acquiring twice would strand the first slot.

**Functionality notes:**
- **The 1.x fallback** is removed entirely, along with `Behavior.Use1xFallback`, the legacy nudges and the expression transpose. You identified the core problem: Windhawk stores defaults at install, so the bridge can't tell a default from a choice, and it can't be undone from the settings page. The same applies to the `minimumTrayHeight = 0` case. Because Windhawk has no API to write settings, any bridge leaves the UI showing one value while the mod uses another. The README now says settings reset once on update and how to carry them across.
- **Restore order on a Grid tray:** the host marker is now inserted at the host's live index on both panel kinds, and the host is re-inserted at the marker's index on both. Child order is therefore exact after a disable/re-enable, including keyboard/UIA order.
- **Drift-check cost:** the throttle is raised to 500 ms, as described above.

Generated by AI. Model: Claude Opus 5


===== windhawk-reviewer[bot] 2026-09-21T18:05:06Z
<!-- ai-review sha=2f2a7da70fbb6f24393d7b7929327e69f2facbae -->

### Submission review

_Note: This review was done by Claude. Due to the amount of submissions, doing a fully manual review for each pull request is no longer feasible. Thank you for understanding._

Remember: The AI reviewer can be wrong - it may misread code, flag correct code as broken, or suggest changes that make things worse. Treat its findings as suggestions to verify, not instructions to follow blindly. You're responsible for the code you submit, so if a finding doesn't hold up, say so instead of changing working code to satisfy it.

Please address the following issues. The items in the collapsed sections are optional, so it's your call whether to address them.

---

No blocking issues — looks good to merge.

The round-4 regression is fixed: `g_stoodDown` is now set only for the vertical taskbar (`tray-utility-customizer.wh.cpp:3398`), every utility off (`:3477`) and a *measured* tray below the minimum (`:3463`), while "not laid out yet" (`:3453`), "no items yet" (`:3486`) and "no placements yet" (`:3608`) return `false` and keep the 20 × 1.5 s retry alive. The other items check out against the source as well: `RetryLoop::Start` publishes by `std::exchange` under the mutex and stops whatever it displaced (`:1638-1648`), so the concurrent-`Start()` window is closed (including the `Wh_ModAfterInit` → `TrayUI::StartTaskbar` overlap at process start, where the two callers are on different threads); `Dispatch::ran` (`:1288`, `:1333-1334`) stops two in-flight `WH_CALLWNDPROC` hooks from running the same callback twice; `AcquireAt` releases a stale same-named marker and corrects the requested slot (`:1891-1902`); `CaptureHost` inserts the marker at the host's live index and `ReturnHostToTray` re-inserts at the marker's index on both panel kinds (`:3199-3202`, `:3219-3223`), so child order survives a disable/re-enable on a `Grid` tray too; `Wh_ModSettingsChanged` loads settings when there is no taskbar thread (`:4160`); `Wh_ModAfterInit` uses the bounded retry (`:4150`); and the 1.x fallback is gone with the README updated to match. The settings block and `LoadSettings` are in sync (every declared key is read with the matching API, nothing undeclared is read), the symbol strings match the 24+ mods hooking the same `taskbar.dll` functions, and the unload path still stops and joins the retry thread before tearing down every XAML callback on the UI thread.

<details><summary>Optional improvements</summary>
<p>

Minor polish — none of this affects users, so it's your call.

* **`sio::LoadString` is dead code, and its name is a Win32 macro.** `tray_utility_settings::LoadString` (`tray-utility-customizer.wh.cpp:463-470`) is never called — `LoadSettings` still goes through `ReadStringSetting` + `CopyStringSetting` (`:2594-2607`, `:2655`), which does the same job plus the truncation log. Because `<windows.h>` is included first, the preprocessor also rewrites the identifier to `LoadStringW`, which is confusing to anyone grepping for it. Drop one of the two: either delete `LoadString`, or make it the one string reader (add the truncation log to it) and remove `ReadStringSetting`/`CopyStringSetting`.
* **Stale comment on `RetryLoop`.** `:1591-1592` says `Start()` is not serialized against a concurrent `Start()` "because both of this mod's callers run on the taskbar's UI thread", but `Wh_ModAfterInit` (`:4150`) calls it from Windhawk's thread and can overlap a `TrayUI::StartTaskbar` rebuild at process start. The exchange-based publish handles that correctly now, so only the comment needs to say so — the next reader will otherwise conclude the exchange is unnecessary.

</p>
</details>

<details><summary>Functionality notes</summary>
<p>

Non-critical observations and ideas about the feature behavior itself.

* **The "no placements yet" exit isn't a measuring state, and nothing observes the tray afterwards.** `NaturalWidth` floors at 24 px (`:2897-2900`), so a present item never resolves to an empty size; `placements.empty()` at `:3608` is only reached when a *written* arrangement with `Layout.NewItems = ignore` names none of the utilities Windows is currently showing (e.g. `Arrangement = "touchKeyboard"` while the keyboard is hidden). That is a settled state, so the 20-attempt retry just logs "Layout produced no placements yet" for 30 s. More importantly, the `LayoutUpdated` drift check and the candidate counts are only registered on a successful apply (`:3944-4016`), and `ResolveLayoutItems` only watches hosts of items it *found* (`:3056-3059`), so when the named utility later appears the mod doesn't notice until a settings change or Explorer rebuild. Consider registering the drift check (with candidate counts) on this path too — `RestoreLayout` already revokes it unconditionally at `:3262`, and the lambda's `!g_layoutApplied` early-out at `:3969` would need a "watching only" state — and then treating the exit as settled.
* **The lone-icon `MainStack` fallback can double-claim a host.** At `:3033-3046`, when no `IconView` matches `emoji` and `MainStack` shows exactly one icon, the whole `MainStack` becomes the `emoji` leaf. If that single icon is actually the touch keyboard (emoji hidden), `touchKeyboard` then matches the same icon inside `MainStack` (`:3018-3031`), both items claim the host, the per-host loop takes the leaf-host branch (`:3895-3906`) and the keyboard's placement is left as an empty slot. Requiring that the lone icon matches no *other* token before adopting it as `emoji` would close that.
* **Candidate counts trip on the microphone/location indicators.** Those in-use indicators are `IconView`s in `MainStack` alongside the utilities (see [taskbar-tray-system-icon-tweaks.wh.cpp](https://github.com/ramensoftware/windhawk-mods/blob/main/mods/taskbar-tray-system-icon-tweaks.wh.cpp#L497-L511)), so an *unmanaged* `MainStack` (`:4005-4015`) now schedules a full restore + reapply every time a mic session starts or stops, even though nothing about the utilities changed. The result is identical, so it's churn rather than a bug; comparing the count of utility-*matching* visible icons (or restricting the check to hosts that can carry an enabled token) would avoid it.

</p>
</details>

<details><summary>Code overview</summary>
<p>

For the maintainer: a map of the mod and what it touches outside its own process. Descriptive only — anything that needs a change is listed above. Line numbers refer to the mod source at commit `2f2a7da`.

**Code regions**

* Line 1–11: metadata — `@include explorer.exe`, `@architecture x86-64`, version 2.0 (changed).
* Line 13–230: README — screenshots, "Upgrading from 1.x" (settings reset once; no 1.x keys are read), arrangement grammar, position/detection notes, settings table, changelog (changed).
* Line 232–372: settings block — grouped `Placement` / `Content` / `Layout` / `Size` / `Adjust` / `Behavior`; `$options` on the string settings only (changed).
* Line 374–407: includes and WinRT/XAML namespaces.
* Line 414–472: `tray_utility_settings` — clamped int/bool reads, `LoadChoice` (`$options` token → enum via `WindhawkUtils::StringSetting`), unused `LoadString` (changed).
* Line 474–1055: `tray_utility_layout` — depth-limited arrangement parser with positioned errors, memoized measure/arrange, `RowsInHeight` in DIPs, auto shape + expression builder, `MissingTokens`/`AppendMissing`, `ResolveArrangement` (`auto` detection).
* Line 1057–1117: `tray_utility_tree_walk` — depth-bounded visual-tree visit / find / collect.
* Line 1119–1207: `tray_utility_property_lease` — snapshots `ReadLocalValue` per (object, property) before the first write; restores the exact local value or `ClearValue`s on unload.
* Line 1209–1244: `tray_utility_taskbar_window` — finds this process's `Shell_TrayWnd` via PID-filtered `EnumWindows`; validates a cached handle.
* Line 1246–1351: `tray_utility_dispatch` — runs a callback on the taskbar UI thread: `WH_CALLWNDPROC` hook scoped to that thread + `RegisterWindowMessage`d private message + `SendMessage`; message checked before `lParam` is trusted; `Dispatch::ran` dedupes concurrent hooks (changed).
* Line 1353–1473: `tray_utility_taskbar_xaml` — `taskbarDllHooks` for `taskbar.dll` (loaded with `LOAD_LIBRARY_SEARCH_SYSTEM32`): `CTaskBand::GetTaskbarHost`, `TaskbarHost::FrameHeight`, `_Ref_count_base::_Decref`, `TrayUI::StartTaskbar` (hooked → rebuild callback); `GetTaskbarXamlRoot` walks CTaskBand → TaskbarHost → FrameworkElement, offset parsed from the `FrameHeight` prologue (x64/ARM64).
* Line 1475–1565: `tray_utility_taskbar_metrics` — taskbar rect → DIPs via `GetDpiForWindow`; vertical taskbar detected by aspect → stand down.
* Line 1567–1726: `tray_utility_retry` — `RetryLoop`: one worker thread per `shared_ptr<Run>`, manual-reset stop event, `MsgWaitForMultipleObjects` wait that pumps sent messages, exchange-based publish that stops a displaced run (changed).
* Line 1728–2035: `tray_utility_slot_lease` — leases a `Grid` column or `StackPanel` index tracked by a named zero-size marker; `AcquireAt` releases a stale same-named marker first (changed); `PlaceChild`, `Release` shift columns back.
* Line 2037–2333: `tray_utility_start_placement` — overlays the group on the taskbar `RootGrid` beside Start, pushes `TaskbarFrameRepeater` margin / Start `RenderTransform`, re-positions on `LayoutUpdated`, restores exact local values on release.
* Line 2337–2348: namespace aliases.
* Line 2350–2510: mod enums, fixed-buffer `Settings`, globals — `[[clang::no_destroy]]` XAML holders and `optional<>` containers, `g_candidateCounts` (weak refs; new), `DispatcherTimer`s, `g_stoodDown` / `g_treeStale` / `g_layoutApplied` flags.
* Line 2512–2592: `ScheduleReapply` (150 ms one-shot `DispatcherTimer`), watcher/callback teardown, `WatchHostVisibility` (`VisibilityProperty` callbacks).
* Line 2594–2677: `LoadSettings` — all grouped 2.0 keys; 1.x fallback removed (changed).
* Line 2679–2714: exception logging for UI callbacks; `RunFromWindowThread` / `GetTaskbarXamlRoot` wrappers.
* Line 2716–3131: utility discovery — `IconView` matching by Segoe glyph, runtime data-model class, AutomationId or XAML name; candidate hosts; `ResolveLayoutItems` (incl. lone-icon `MainStack` fallback); diagnostics.
* Line 3133–3323: snapshot/restore — `TrackPlacement`, `CaptureHost` (marker inserted at host's live index, changed), `ReturnHostToTray` (re-insert at marker index, changed), `RestoreHostPosition`, `RestoreLayout` (revoke callbacks → restore properties → return hosts → drop group → release lease → remove markers).
* Line 3325–4065: `ApplyLayout` — stale-tree handling, restore, orientation and height checks (transient vs. settled, changed), item resolution, arrangement compute (auto / parse-error fallback / append missing), group creation and placement (Start lane, leased slot or borrowed anchor slot), host reparenting and per-icon margin placement, candidate-count recording (new), throttled `LayoutUpdated` drift check (500 ms, changed), Start settle timer.
* Line 4067–4131: `ApplyLayoutOnWindowThread`, `g_retry` and retry budget constants (changed), `LayoutIsApplied`, `OnTaskbarRebuilt`.
* Line 4133–4238: `Wh_ModInit` (settings + hooks), `Wh_ModAfterInit` (bounded retry, changed), `Wh_ModSettingsChanged` (stop retry; load + reapply on the UI thread, or load directly when no taskbar, changed), `TearDownOnWindowThread`, `Wh_ModUninit` (stop/join retry thread, dispatch teardown to the UI thread with one rediscovery retry).

**Side effects of interest**

* Line 1390–1391: other: `LoadLibraryExW(L"taskbar.dll", nullptr, LOAD_LIBRARY_SEARCH_SYSTEM32)` in `Wh_ModInit` — System32-restricted; the module is already loaded in explorer.exe.
* Line 1219–1232: other: `EnumWindows` filtered to `GetCurrentProcessId()` to find `Shell_TrayWnd`, on every taskbar lookup.
* Line 1320–1347: other: `SetWindowsHookExW(WH_CALLWNDPROC, …, threadId)` on this process's taskbar UI thread, plus `SendMessageW` of a `RegisterWindowMessage`d message to the mod's own process's `Shell_TrayWnd`; the hook is removed as soon as the `SendMessage` returns. Runs per dispatch (retry attempts, settings change, uninit).
* Line 1441–1443: other: reads the `TaskbandHWND` window property and `GetWindowLongPtr(…, 0)` of the in-process task band window to reach the taskbar XAML root.
* No file, registry, network, IPC or system-settings access — everything else is XAML tree manipulation inside explorer.exe, restored on unload.

</p>
</details>

---

**Next steps:**

* `/ai-review` - after pushing fixes, to get a review of the updated code. You can repeat this as many times as you need, but each review is thorough and usually there's no need for more than 2-3 iterations.
* `/ready-for-reviewer` - once you're satisfied with the state of the pull request, to hand it over to a human reviewer. If some findings above are left unaddressed, add a short note explaining why.

See the [review process](https://github.com/ramensoftware/windhawk/wiki/Mod-submission-review-process) for details.

Important: Each reply that was generated by AI must be disclosed by including the following text in the reply: "Generated by AI. Model: (ai-model-name)".

