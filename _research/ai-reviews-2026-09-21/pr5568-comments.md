===== sb4ssman 2026-09-21T17:51:30Z
/ai-review

===== sb4ssman 2026-09-21T17:52:29Z
Thanks — all four were real. Addressed in this push:

**1(a). Unload no longer gives up.** The `attempt < 50` cap is gone. Uninit loops `EndMenu` + `WM_CANCELMODE` until the menu path is idle, exactly as you suggested. Each `RunFromWindowThread` is a `SendMessage`, so the loop follows the taskbar thread's real progress. The give-up branch, the "retaining the idle event" leak, and the conditional `CloseHandle` are all gone.

**1(b). The queued Shell verb is now inside the wait.** There is a shared RAII `MenuPathScope` that owns the `fetch_add`/`ResetEvent` … `fetch_sub`/`SetEvent` pairing. `ShowFolderMenu` holds it for its whole tail, and `MenuOwnerSubclassProc` holds it around `InvokePendingShellCommand`. A Delete confirmation or Open With dialog now keeps unload waiting until it is answered.

Following the same reasoning turned up one more window, now closed. Both sites read `g_unloading` *before* opening their scope. Uninit could store the flag and read a depth of zero in between. Then `TrackPopupMenu`'s loop would pump uninit's own cleanup dispatch and return into a freed image. Both sites now open the scope first and check `g_unloading` after it. Uninit stores the flag before reading the depth, so with seq_cst atomics one side always sees the other.

**2. Native icons on a running Explorer / on settings change.** We took your simpler option. `Wh_ModAfterInit` and `Wh_ModSettingsChanged` no longer apply directly; the worker's first attempt (no delay) runs `PrepareFolderIcons()` before the first build. This also removes the label→icon flash.

**3. `LoadLegacyFolders`.** Removed, together with the whole 0.7 bridge (`HasLegacySettings`, `PreferCurrentOrLegacy*`, `Behavior.UseLegacySettings`). Windhawk has no API to write a setting, so a bridge always leaves the settings page showing a value the mod isn't using. The "prefer legacy while the 2.0 value equals its default" rule also meant a carried value could never be set back to its default (your functionality note and the clamp note both describe symptoms of this). The README now says plainly that settings reset once on update, and how to copy them across.

**4. README.** The `|` in the Arrangement table is escaped. "Upgrading from 0.7" describes the one-time reset and the 150-item default. The `ShowHidden` row says hidden-only.

**Optional items taken:**
- Dead template code: the mod is now assembled from eight small components, with its own focused tree walk. `GetMetrics`/`LayoutModelApplies`/`OrientationName`, the unused layout helpers, `sio::StringSetting`, the `vtw::*` entry points and `igc::Acquire` are no longer in the file, and neither is the commentary about other mods and lab scripts.
- The retry thread now **uses** `RetryLoop` rather than carrying it as dead code. That closes the concurrent-`StopRetryThread` hole you described: every caller that sees a live run waits for it. It runs 40 attempts 1.5 s apart, the same one-minute window for slow sign-ins. A vertical-taskbar stand-down counts as settled, so it does not retry.
- `GetSystemMetricsForDpi` is called directly.
- `LoadFolders` uses the variadic `GetStringSetting`. `Placement.Position`, `FillOrder`, `Justify` and `NewItems` are parsed once through `LoadChoice`; position is now an enum, with no string compares left.
- The Desktop-root sort breaks display-name ties by `fileName`, so the dedupe is exact.

Every item from this round is taken; nothing is left unaddressed.

Generated by AI. Model: Claude Opus 5


===== windhawk-reviewer[bot] 2026-09-21T18:16:02Z
<!-- ai-review sha=20abef99d9f85ff113fc73c3d9e225ab00f910ab -->

### Submission review

_Note: This review was done by Claude. Due to the amount of submissions, doing a fully manual review for each pull request is no longer feasible. Thank you for understanding._

Remember: The AI reviewer can be wrong - it may misread code, flag correct code as broken, or suggest changes that make things worse. Treat its findings as suggestions to verify, not instructions to follow blindly. You're responsible for the code you submit, so if a finding doesn't hold up, say so instead of changing working code to satisfy it.

Please address the following issues. The items in the collapsed sections are optional, so it's your call whether to address them.

---

All four items from the last round check out against the source: the unwind loop in `Wh_ModUninit` has no cap and `CloseHandle` is unconditional; `MenuPathScope` brackets both `ShowFolderMenu`'s whole tail and the posted Shell verb in `MenuOwnerSubclassProc`, with the scope opened before `g_unloading` is read at both sites; `Wh_ModAfterInit` / `Wh_ModSettingsChanged` go through the worker so `PrepareFolderIcons` precedes the first build; the 0.7 bridge is gone and the README matches the code. `RetryLoop` is now actually used, `LoadChoice` covers every dropdown, and the Desktop-root sort has the `fileName` tie-break. One thing remains, and it follows from the new worker ordering.

**1. The `TrayUI::StartTaskbar` hook blocks the taskbar thread on the icon worker.** `TrayUI_StartTaskbar_Hook` → `StartRetryThread` → `RetryLoop::Start` → `Stop()` → `StopRun`, which waits (`MsgWaitForMultipleObjects`, `INFINITE`) for the current worker thread to exit — on the taskbar's UI thread, inside Explorer's own taskbar construction. Each worker attempt is `PrepareFolderIcons()` then apply, and `PrepareFolderIcons` only checks `g_unloading` between entries, never the run's stop event, so the UI thread sits there until every `CacheFolderIcon` (`SHParseDisplayName` → `SHCreateItemFromIDList` → `IShellItemImageFactory::GetImage`) has returned. At Explorer start this is the normal ordering — `Wh_ModAfterInit` has just started the worker when `StartTaskbar` fires — and the taskbar rebuilds this hook exists for restart the worker, so it recurs. With local targets that's tens of milliseconds; with a native-icon entry pointing at an unreachable UNC path it is the SMB connect timeout (tens of seconds) with the taskbar thread frozen — and because a failed extraction is not cached, the same target is probed again on every `Start` and every retry attempt. Keeping extraction off the UI thread was the point of the worker, so the UI thread should not wait on it:

- Don't stop-and-restart from the hook. Only Windhawk's thread ever needs to *wait* (uninit, settings change). From `StartTaskbar`, clear `g_injectionLive` and, if a run is live, just wake it (a manual-reset "retry now" event the loop waits on alongside `stopEvent`, so it skips the 1.5 s interval); start a new run only when none is live.
- Independently, make the worker interruptible and the probe bounded: check the run's stop event between entries in `PrepareFolderIcons` (pass it to the attempt, or expose a `StopRequested()`), and remember failed targets for the lifetime of the loaded settings so an unreachable path is probed once rather than on every attempt and rebuild.

<details><summary>Optional improvements</summary>
<p>

Minor polish — none of this affects users, so it's your call.

* **Dead code and stale comments that survived the component split.** `igc::Acquire` (`taskbar-folder-menus.wh.cpp#L1401`) is still there and still uncalled (the mod goes `ResolveSlot` + `AcquireAt`); `ngl::RowsInHeight` (`#L992`) is unused while `BuildFolderButtonGrid` recomputes the same formula inline at `#L3204`; `sio::LoadString` (`#L408`) has no caller; `g_injectedSlot` (`#L2146`) is written in five places and never read; `forceFirstAttempt` is never passed as `true`. Comments carried over from the tray-utility mod no longer describe this file: `#L1085-L1106` talk about "utilities", the touch keyboard, `chevron`/`overflow` aliases and a `SameUtility` that doesn't exist here; `#L2378-L2383` still explain the dynamic `GetSystemMetricsForDpi` resolution the code no longer does; `#L1781-L1782` say "both of this mod's callers run on the taskbar's UI thread", but `Wh_ModAfterInit` and `Wh_ModSettingsChanged` call `Start` from Windhawk's thread (the `displaced` exchange handles it — the comment is just wrong).
* **`[[clang::no_destroy]]` on `g_retry` (`#L3741`) isn't needed.** After `Stop()` the object holds nothing, and even on the process-termination path its destructor is a `CloseHandle` pair plus a heap free — safe at shutdown, so the attribute suppresses nothing and only invites copying it onto types where it matters. See https://github.com/ramensoftware/windhawk/wiki/Global-objects-and-process-shutdown for which globals actually need it (the `std::optional` wrappers on `g_buttonEventStates` / `g_appPlacement` and the bare attribute on `g_buttonGrid` are the right shapes).
* **Includes.** `std::isfinite` (`#L768`) needs `<cmath>`; it currently compiles via transitive includes. `<winrt/Windows.UI.Xaml.Controls.Primitives.h>` and `<winrt/Windows.UI.Xaml.Input.h>` (plus the `using namespace winrt::Windows::UI::Xaml::Input`) appear unused.
* **`Wh_ModInit` leaks `g_menuIdleEvent` on the `HookTaskbarDllSymbols` failure path** (`#L3795-L3798`). Windhawk re-runs init after each settings change when it returned `FALSE`, so that's one handle per attempt; close it before returning.

</p>
</details>

<details><summary>Functionality notes</summary>
<p>

Non-critical observations and ideas about the feature behavior itself.

* **`PositionAfterAppIcons` runs on every `LayoutUpdated` of the taskbar frame's `RootGrid`** (`#L3440`) and walks every repeater child with `TransformToVisual` each time. `LayoutUpdated` fires for any layout pass anywhere in that subtree (hover animations, badge updates), so a cheap pre-check — skip when the repeater's `ActualWidth` and child count haven't changed since the last pass — would remove most of the work without changing behavior.

</p>
</details>

<details><summary>Code overview</summary>
<p>

For the maintainer: a map of the mod and what it touches outside its own process. Descriptive only — anything that needs a change is listed above. Line numbers refer to the mod source at commit `20abef9`.

**Code regions**

* Line 1–11: metadata — `explorer.exe` only, `x86-64`, links ole32/oleaut32/runtimeobject/shell32/shlwapi/uuid/gdi32/comctl32. *(changed: version 0.7 → 2.0)*
* Line 13–170: README — screenshots (raw.githubusercontent.com), arrangement grammar, settings table, 0.7 upgrade note. *(changed)*
* Line 172–307: settings block — grouped `Placement` / `Content` (folder array with Label/Target/UseDefaultIcon) / `Layout` / `Size` / `Adjust` / `Surface` / `Behavior`; all keys are read by `LoadSettings`. *(changed)*
* Line 309–351: includes and `using namespace` for XAML.
* Line 353–416: `folder_menus_settings` — clamped int/bool reads and the `LoadChoice` token table used for every `$options` dropdown. *(new)*
* Line 418–597: `folder_menus_button_surface` — parses `#RRGGBB`/`#AARRGGBB`/`accent*`/`transparent` into brushes, optional gradient shine, applies foreground/background/border/corner/opacity resources to a XAML `Button`. *(new)*
* Line 599–1180: `folder_menus_layout` — parser for the `Arrangement` expression (`|` / `,` / parentheses / `[dx,dy]`, depth-limited), memoized measure + arrange into placements, auto rows×columns shape, appending of folders the expression didn't name. *(new)*
* Line 1182–1489: `folder_menus_slot_lease` — classifies `SystemTrayFrameGrid` as `Grid` (lease = column) or `StackPanel` (lease = child index), resolves anchors by named tray children (`ControlCenterButton`, `NotificationCenterButton`, `ShowDesktopStack`), inserts/removes a zero-size marker and shifts sibling columns/spans; adopts and releases a stale marker from a previous instance. *(new — the 26200.9457 fix)*
* Line 1491–1526: `folder_menus_taskbar_window` — `EnumWindows` filtered to this PID for `Shell_TrayWnd`, cached-handle validation with `IsWindow`.
* Line 1528–1633: `folder_menus_dispatch` — `RunFromWindowThread` via a thread-local `WH_CALLWNDPROC` hook + `RegisterWindowMessage`'d message, with a `ran` flag against concurrent dispatches. *(changed)*
* Line 1635–1755: `folder_menus_taskbar_xaml` — `taskbarDllHooks` (`taskbar.dll`, resolved once): `CTaskBand` `ITaskListWndSite` vftable, `CTaskBand::GetTaskbarHost`, `TaskbarHost::FrameHeight`, `_Ref_count_base::_Decref`, and a hook on `TrayUI::StartTaskbar` that calls the original then the mod's rebuild callback; reads the `FrameworkElement` offset from `FrameHeight`'s prologue (x64 and ARM64) to reach the taskbar `XamlRoot`.
* Line 1757–1916: `folder_menus_retry` — `RetryLoop`: a worker thread per `Start` owning a stop event through a shared `Run`, `Stop` waits with `MsgWaitForMultipleObjects` pumping sent messages, `Start` exchanges the published run and waits for whatever it displaced. *(changed — now the mod's actual retry mechanism)*
* Line 1929–2135: settings structs, `Trim` / `ExpandEnv` (`%DESKTOP%` / `%DOWNLOADS%` / `%DOCUMENTS%` tokens via `SHGetKnownFolderPath`, then `ExpandEnvironmentStringsW`), `LoadFolders` (defaults to Desktop + Control Panel when empty), `LoadSettings`. *(changed)*
* Line 2137–2233: globals — `g_unloading` / `g_updatingSettings`, `[[clang::no_destroy]]` XAML holders, menu-path depth counter + idle event, `MenuPathScope` RAII, per-menu PIDL/bitmap/submenu state, pending Shell verb, the registered invoke message. *(changed)*
* Line 2235–2280: thin wrappers, `FindChildRecursive` (visual-tree DFS, depth 20), `FindLiveSystemTrayFrameGrid`.
* Line 2282–2611: menu content — `ParseShellTarget` (`shell:Desktop`, Control Panel CLSID fallbacks, `SHParseDisplayName`), `BindFolderFromPidl`, `StrRetToBufW`, per-item icon bitmaps at the taskbar's DPI, `EnumerateShellFolder` (folders first, Desktop-root dedupe by parsing name, `MaxMenuItems` cap), `InsertShellMenuItem` with lazily populated submenus limited by `MaxDepth`.
* Line 2613–2681: "Open in Explorer" header, `InvokePidl` (`ShellExecuteExW` "open"), queued Shell-verb `InvokeCommand`.
* Line 2683–2846: Shell context menu — `IContextMenu` from the item's parent folder, `QueryContextMenu` (`CMF_EXTENDEDVERBS` with Shift), nested `TrackPopupMenuEx(TPM_RECURSE)`, outside-click detection, deferral of the chosen verb until the outer popup has unwound.
* Line 2848–2974: `WH_MSGFILTER` proc (populates pending submenus on `WM_INITMENUPOPUP`, detects outside clicks, defers right-click to a posted `WM_MENURBUTTONUP`) and `MenuOwnerSubclassProc` on `Shell_TrayWnd` (runs the posted verb inside a `MenuPathScope`, handles `WM_MENURBUTTONUP`, forwards owner-draw messages to `IContextMenu2/3`). *(changed)*
* Line 2976–3093: `ShowFolderMenu` — re-entry guard, builds the popup, `SetForegroundWindow` + edge-aware `TrackPopupMenu` alignment, `SetWindowSubclass` + filter hook, `TrackPopupMenu` inside `MenuPathScope` with a post-scope `g_unloading` re-check, teardown and verb posting. *(changed)*
* Line 3095–3329: icon pixel cache (`PrepareFolderIcons` on the worker in an STA, `IShellItemImageFactory::GetImage` → `GetDIBits`, ≤128 entries), `NativeFolderIcon` → `WriteableBitmap`, `BuildFolderButtonGrid` (arrangement → `Grid` of `Button`s, tooltip/automation names, click handler that opens the menu and resets the button's visual state). *(new)*
* Line 3331–3447: after-app-icons placement — appends the group to the `TaskbarFrame` `RootGrid`, widens `TaskbarFrameRepeater`'s right margin to reserve room, repositions on `LayoutUpdated`, hides when it can't fit before the tray, restores the margin on release. *(new)*
* Line 3449–3542: grid removal/lookup in the tray panel, Grid-only column collision detection/repair, `ClearButtonEventState` (revokes Click tokens, clears tooltips/content), `RemoveButtonGrid`. *(changed)*
* Line 3544–3626: `InjectButtonGrid` — classifies the tray panel, resolves the anchor slot (logs class + named children on failure), builds, leases, places. *(changed)*
* Line 3628–3723: `ApplyAllSettings` (vertical-taskbar stand-down, orphan/collision reconciliation, after-app-icons branch, inject) and its UI-thread dispatcher.
* Line 3725–3762: `taskbar.dll` hook install, `g_retry` with `StartRetryThread` (40 attempts × 1.5 s; each attempt = prepare icons, then apply on the UI thread) / `StopRetryThread`. *(changed)*
* Line 3764–3897: `LogUiCallbackFailure`, `Wh_ModInit` (settings, idle event, symbol hooks), `Wh_ModAfterInit` (starts the worker), `Wh_ModUninit` (stops the worker, loops `EndMenu` + `WM_CANCELMODE` until the menu path is idle, removes hook/subclass/grid and resets the XAML holders on the UI thread, closes the event), `Wh_ModSettingsChanged` (stops the worker, removes the grid on the UI thread, reloads, restarts the worker). *(changed)*

**Side effects of interest**

* Line 1672–1673: other reach outside the process: `LoadLibraryExW(L"taskbar.dll", nullptr, LOAD_LIBRARY_SEARCH_SYSTEM32)` in `Wh_ModInit`; System32-restricted, never freed (the DLL is resident in Explorer anyway).
* Line 1499–1516: other reach outside the process: `EnumWindows` filtered by `GetCurrentProcessId()` to find `Shell_TrayWnd`; on every apply, dispatch and teardown.
* Line 1602–1629, 3037–3039: other reach outside the process: thread-local `SetWindowsHookExW` — `WH_CALLWNDPROC` on the taskbar thread for the duration of one `RunFromWindowThread`, `WH_MSGFILTER` on the taskbar thread while a folder popup is open; both unhooked on the same path and again in `Wh_ModUninit`.
* Line 3010: other reach outside the process: `SetForegroundWindow(Shell_TrayWnd)` before `TrackPopupMenu` (own process, standard for taskbar popups).
* Line 2635–2644, 2674–2675: other reach outside the process: `ShellExecuteExW` "open" on the clicked item, and `IContextMenu::InvokeCommand` of whatever verb the user picked from the native Shell context menu (may be Delete, Open with, Properties…); both only on an explicit user click.
* Line 2001–2015, 2314–2344, 2462–2563, 2435–2446, 3121–3158: external file read: Shell namespace enumeration and icon extraction of the user-configured targets (`SHParseDisplayName`, `IShellFolder::EnumObjects`, `SHGetFileInfoW`, `IShellItemImageFactory::GetImage`); read-only, on menu open (UI thread) and in the icon worker; a UNC target implies SMB traffic.
* No registry access, no network beyond the Shell reads above, no IPC, no system-setting changes, no file writes. All XAML edits (tray column/child lease, `TaskbarFrameRepeater` margin) are in-process and reverted in `Wh_ModUninit`.

</p>
</details>

---

**Next steps:**

* `/ai-review` - after pushing fixes, to get a review of the updated code. You can repeat this as many times as you need, but each review is thorough and usually there's no need for more than 2-3 iterations.
* `/ready-for-reviewer` - once you're satisfied with the state of the pull request, to hand it over to a human reviewer. If some findings above are left unaddressed, add a short note explaining why.

See the [review process](https://github.com/ramensoftware/windhawk/wiki/Mod-submission-review-process) for details.

Important: Each reply that was generated by AI must be disclosed by including the following text in the reply: "Generated by AI. Model: (ai-model-name)".

